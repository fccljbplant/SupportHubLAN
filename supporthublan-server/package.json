{
  "name": "supporthublan-server",
  "version": "2.0.0",
  "description": "SupportHubLAN Backend — Windows endpoint management via PsTools. Encrypted JSON storage, no native dependencies.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js --dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "ws": "^8.14.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
